开始一个新的高保真复刻项目。

请严格执行已安装的 Sci PS skill v5.0 单入口状态机：
- 自动读取/创建 project_state；
- 自动先做 Photoshop MCP 快速能力检查；
- 已有能力就直接进入项目初始化；
- 没有能力就先引导安装和验证；
- 不要让我自己选择内部 MD、prompt、schema 或脚本；
- 接收并保存参考图后，不要在 Codex 内执行语义锁定、拆分参考图、建立组件/批次计划或动态生成项目专用 MD；
- 保存参考图后只做一次非阻塞生产路线提示：明显手绘/扁平矢量风格可询问我是否已有 Illustrator 或 SVG/AI/PDF；没有时不得暂停，所有 Photoshop 不适合稳定原生绘制的复杂元素默认交给 Image2；
- 直接告诉我把参考图和 Skill 内置通用 MD 交给网页版 GPT / Image2，并从 `STAGE 1 / 6 — REFERENCE ANALYSIS` 开始，先完成动态语义识别与确认，再执行组件拆分和完整六阶段；
- 告诉我生成好的 `COMPONENT_HANDOFF.zip` 放到当前项目哪个目录；
- 我回来只需要说“组件包已放入”；
- 组件包通过后直接进入 Photoshop MCP 真实逐层构建；
- Photoshop 必须作为最终高级视觉处理中心，重点完成真实蒙版、遮挡、材质融合、光效、阴影、渐变、背景环境光场、局部调整和整体调色；
- 参考图只能对照，不能充当成品；
- 每一步都必须新增并验证真实 Photoshop 图层/图层组；
- 必须生成视觉差异报告，完成“对照—修正—复检”闭环；只有 `FINAL_VISUAL_MATCH = TRUE` 且参考图层已隐藏后，才能让我验收。
- 能力预检必须检查完整生产工具，不能只用 `ps_health` 或历史 POC 放行；最终必须绑定真实 Photoshop 导出图、文件哈希、像素比较和图层可见性证据。

现在请先告诉我：当前阶段、当前能力状态、你需要我提供的第一项内容。
